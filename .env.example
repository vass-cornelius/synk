# -- Moco Configuration --
MOCO_SUBDOMAIN="one-inside"

# Your Moco API Key. Find it in Moco under your Profile -> API Access (https://one-inside.mocoapp.com/profile/integrations)
MOCO_API_KEY="your-moco-api-key"

# -- JIRA Configuration --
# 1. List the short names of your JIRA instances, separated by commas.
#    Example: JIRA_INSTANCES="work,client_a"
JIRA_INSTANCES="VASS"

# Your company's JIRA URL
JIRA_VASS_SERVER="https://chvasscompany.atlassian.net"

# The email you use to log in to JIRA
JIRA_VASS_USER_EMAIL="your-email@example.com"

# Your JIRA API Token. Generate one here: https://id.atlassian.com/manage-profile/security/api-tokens
JIRA_VASS_API_TOKEN="your-jira-api-token"

# Comma-separated list of project keys, e.g., "SYN,DEVOPS,GVBDEV"
JIRA_WORK_PROJECT_KEYS="GVBDEV"

# -- Optional: Default Task Name --
# Use a regex pattern to match your most common task.
# Example: To match "CH: Main | ZT/...", use "^CH: Main"
DEFAULT_TASK_NAME="^CH: Main"

# -- Optional: Task Filter (exclude) --
# Use a regex pattern to exclude tasks from the displayed task lsit.
# Example: To match "MK: lorem ipsum | ZT/...", use "^MK:"
TASK_FILTER_REGEX="^MK:"

# -- Workflow Configuration --
# Define the order of questions. Valid keys: project, task, jira, comment, time
QUESTION_ORDER="project,task,jira,comment,time"

# -- Duration Rules (in minutes) --
# Global minimum/maximum duration for a single time entry.
# Leave empty for no limit.
MIN_DURATION_MINUTES="15"
MAX_DURATION_MINUTES="180"

# Round duration to the nearest increment (in hours).
# e.g., 0.25 rounds to the nearest 15 minutes. Leave empty to disable.
DURATION_ROUNDING_INCREMENT="0.25"

# Per-project duration rules (optional, overrides global settings).
# This must be a valid JSON string. The key should match the project display name: "Customer / Project Name".
# Example: PROJECT_DURATION_RULES='{"Internal / Synk": {"min": 5, "max": 120}, "Big Client / Website Relaunch": {"min": 15}}'
PROJECT_DURATION_RULES='{}'